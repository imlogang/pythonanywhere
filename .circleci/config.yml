version: 2.1
orbs:
  python: circleci/python@2.0.3

workflows:
  build-app-with-test:
    jobs:
      - build-and-test:
          context: 
            - jeff

jobs:
  build-and-test:
    docker:
      - image: cimg/base:2022.08
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.14
      - run: docker pull localstack/localstack
      - run: docker list images 


