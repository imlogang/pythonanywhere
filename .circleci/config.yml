version: 2.1
orbs:
  python: circleci/python@2.0.3

workflows:
  build-app-with-test:
    jobs:
      - build-and-test

jobs:
  build-and-test:
    macos:
      xcode: 13.4
    resource_class: macos.x86.metal.gen1
    steps:
      - checkout


